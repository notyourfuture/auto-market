import React from "react";
import { BrowserRouter, Routes } from "react-router-dom";

const MyRoutes = () => {
  return (
    <BrowserRouter>
      <Routes></Routes>
    </BrowserRouter>
  );
};

export default MyRoutes;
